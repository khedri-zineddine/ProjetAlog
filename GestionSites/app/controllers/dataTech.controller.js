import {TechData} from '../models'

/****************************************  
    * 
    * Create new site in dataBase
    * @param dataTech instance of TechData
    * @return  return  the created data
    * 
****************************************/
export const createDataTech = (dataTech)=>{
    
}